@using Vfi.Ui.Mvc.Vfi.Areas.Sales.Models
@{
    ViewBag.Title = "Customer Type Management";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="title">Customer-Type Information Management
  @*  Quản lý thông tin loại khách hàng*@</div>

<div class="cover-div">
@{
    Html.Telerik().Grid<CustomerTypeModel>()
        .Name("gridCustomerType")
        .DataKeys(keys => keys.Add(f => f.CustomerTypeId).RouteKey("customerTypeId"))
        .ToolBar(
            toolbar =>
            toolbar.Insert().ButtonType(Telerik.Web.Mvc.UI.GridButtonType.ImageAndText).Text("Add New").ImageHtmlAttributes(new { style = "margin-left:0" }))
        .DataBinding(dataBindings=>dataBindings.Ajax()
            .Select("SelectCustomerType","CustomerType").Insert("InsertCustomerType","CustomerType").Update("UpdateCustomerType","CustomerType")
        )
        .Columns(columns=>
        {
            columns.Bound(f => f.CustomerTypeName).Width(175).Title("Customer Type");
            columns.Bound(f => f.Note).Title("Note");
            columns.Bound(f => f.Active).Width(75).Title("Active")
                .ClientTemplate("<input type='checkbox' disabled='disabled' name='Active' <#=Active? checked='checked' : '' #> />")
                .HtmlAttributes(new { style = "text-align:center" });
            columns.Command(command =>
                command.Edit().ButtonType(Telerik.Web.Mvc.UI.GridButtonType.ImageAndText).HtmlAttributes(new { text = "Sửa"})
            ).Width(98).Title("Command").HtmlAttributes(new { style = "text-align:center" });
        })
        .ClientEvents(e=> e.OnError("grid_OnError"))
        .Editable(editing => editing.Mode(Telerik.Web.Mvc.UI.GridEditMode.InLine))
        .Pageable(page => page.PageSize(10))
        .Sortable()
        .Render();  
}
</div>