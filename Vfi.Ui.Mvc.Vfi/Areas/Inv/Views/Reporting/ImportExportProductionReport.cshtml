@using Vfi.Ui.Mvc.Vfi.Areas.Inv.Models
@model ProductInventoryModel
@{
    ViewBag.Title = "Finished-Products Report (Entry/Release)";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Scripts{


    <!-- grid Monthly -->
    <script type="text/javascript">


        function grid_OnDataBinding(args) {

            if (startGrid && !onLoading) {
                OnLoadingData();
                var productCode = $("#ProductCodeNameForMonthly").data("tAutoComplete").value();

                var customerId = $("#CustomerForMonthly").data('tComboBox').value();
                if (customerId === "")
                    customerId = 0;

                var type = $("#ReportType").data("tDropDownList").value();
                if (type === null)
                    type = 2;

                var fromDate = $("#FromDate").data("tDatePicker").value();
                if (fromDate === null)
                    fromDate = "";
                else
                    fromDate = $.telerik.formatString('{0:dd/MM/yyyy}', fromDate);
                var toDate = $("#ToDate").data("tDatePicker").value();
                if (toDate === null)
                    toDate = "";
                else
                    toDate = $.telerik.formatString('{0:dd/MM/yyyy}', toDate);

                args.data = $.extend(args.data, {
                    type: type,
                    customerId: customerId,
                    productName: productCode,
                    fromDate: fromDate,
                    toDate: toDate
                });
            }
            index = 0;
        }

        var index = 0;

    </script>
    <!-- report -->
    <script type="text/javascript">
        function printImportExportProduction() {

            if (!onLoading) {
                OnLoadingData();
                var type = $("#ReportType").data("tDropDownList").value();
                if (type === null)
                    type = 2;
                var fromDate = $("#FromDate").data("tDatePicker").value();
                if (fromDate === null)
                    fromDate = "";
                else
                    fromDate = $.telerik.formatString('{0:dd/MM/yyyy}', fromDate);
                var toDate = $("#ToDate").data("tDatePicker").value();
                if (toDate === null)
                    toDate = "";
                else
                    toDate = $.telerik.formatString('{0:dd/MM/yyyy}', toDate);

                var productCode = $("#ProductCodeNameForMonthly").data("tAutoComplete").value();

                var customerId = $("#CustomerForMonthly").data('tComboBox').value();
                if (customerId === "")
                    customerId = 0;

                $('#result').load('@Url.Action("PrintFinishedProductionReport", "Reporting")',
                    {
                        productCode: productCode,
                        customerId: customerId,
                        type: type,
                        fromDate: fromDate,
                        toDate: toDate
                    }, function () {
                        OnLoadedData();
                        var window = $('#windowReport').data('tWindow');
                        window.open();
                    });
            }
            return false;
        }

        function reportType_onLoad() {
            var combobox = $('#ReportType').data('tDropDownList');
            var dataSource =
            [
                { Text: "All", Value: "1" },
                { Text: "Developments", Value: "2" }
            ];

            combobox.dataBind(dataSource);
            combobox.select(1);
        }

        function month_onLoad() {
            var combobox = $('#Month').data('tDropDownList');
            var dataSource =
            [
                { Text: "1", Value: "1" },
                { Text: "2", Value: "2" },
                { Text: "3", Value: "3" },
                { Text: "4", Value: "4" },
                { Text: "5", Value: "5" },
                { Text: "6", Value: "6" },
                { Text: "7", Value: "7" },
                { Text: "8", Value: "8" },
                { Text: "9", Value: "9" },
                { Text: "10", Value: "10" },
                { Text: "11", Value: "11" },
                { Text: "12", Value: "12" }
            ];

            combobox.dataBind(dataSource);
            combobox.value(new Date().getMonth() + 1);
        }


        var startGrid = false;
        function RefreshGrid2(name) {
            if (!onLoading) {
                startGrid = true;
                RefreshGrid(name);
            }
        }

        function grid_OnDataBound() {
            OnLoadedData();
        }

    </script>
}

<div class="title">Finished-Products Report (Entry/Release)</div>
<div class="clear"></div>
<br />

<div class="t-info-sha" style="padding-left: 15px; padding-right: 15px;">

    <div class="clear"></div>
    <fieldset>
        <legend>Filters</legend>
        <div class="grid_3">
            @{
                Html.Telerik().DropDownList()
                    .Name("ReportType")
                    .HtmlAttributes(new { style = "width: 100%" })
                    .DropDownHtmlAttributes(new { style = "width: 100px" })
                    //.Filterable(f => f.FilterMode(AutoCompleteFilterMode.Contains))
                    // .HighlightFirstMatch(true)
                    //.AutoFill(true)
                    .ClientEvents(events => events.OnLoad("reportType_onLoad"))
                    .Render();
            }
        </div>
        <div class="grid_2">
            @Html.Label("Customer")
        </div>
        <div class="grid_4">
            @{
                Html.Telerik().ComboBox()
                    .Name("CustomerForMonthly")
                    .DataBinding(binding => binding.Ajax().Select("SelectComboBoxCustomer", "Customer", new { area = "Sales" }))
                    .HtmlAttributes(new { style = "width: 150px" })
                    .DropDownHtmlAttributes(new { style = "width: 275px" })
                    //.ClientEvents(events => events.OnChange("customerForPeriod_onChange"))
                    .AutoFill(true)
                    .Filterable(f => f.FilterMode(Telerik.Web.Mvc.UI.AutoCompleteFilterMode.Contains))
                    .Render();
            }
        </div>
        <div class="grid_1_5">
              @Html.Label("Product Code")
@*            @Html.LabelFor(f => f.ProductCodeName)*@
        </div>
        <div class="grid_4">
            @{
                Html.Telerik().AutoComplete()
                    .Name("ProductCodeNameForMonthly")
                    .DataBinding(binding => binding.Ajax().Select("AutoCompletedProductInventoryByCode", "Inv"))
                    .DropDownHtmlAttributes(new { style = "width: 200px; " })
                    .Encode(false)
                    .AutoFill(true)
                    .HighlightFirstMatch(true)
                    .Render();
            }
        </div>

        <div class="grid_3">
            @{
                Html.Telerik().DatePicker()
                    .Name("FromDate")
                    .Format("dd/MM/yyyy")
                    .Value(DateTime.Now)
                    .Render();
            }
            @{
                Html.Telerik().DatePicker()
                    .Name("ToDate")
                    .Format("dd/MM/yyyy")
                    .Value(DateTime.Now)
                    .Render();
            }
        </div>
        <div class="grid_3">
            <button id="btnRefesh" class="t-button" onclick=" return RefreshGrid2('gridProductionMonthly'); ">
                <span class='t-icon t-insert'></span>
                Find
		<span class="loadButton"></span>
            </button>
        </div>
        <div class="grid_4">

            <button id="btnImportExportProduction" class="t-button" onclick=" return printImportExportProduction(); ">
                <span class='t-icon t-insert'></span>
               Print Report
		<span class="loadButton"></span>
            </button>
        </div>
    </fieldset>
</div>
<div class="clear">
</div>

<div class="cover-div">
    @{
        Html.Telerik().Grid<ImportExportProductionDetailModel>()
            .Name("gridProductionMonthly")
            .DataKeys(keys => keys.Add(f => f.ProductId).RouteKey("productId"))
            .DataBinding(dataBindings => dataBindings.Ajax()
                                                     .Select("SelectFinishedProductionReport", "Reporting",
                                                             new { type = 2, customerId = 0, productName = "", fromDate = "", toDate = "" }))
            .Columns(columns => {
                    columns.Bound(f => f.ProductId).Width(50)
                            .ClientTemplate("<#= ++index #>")
                            .HeaderTemplate("<strong>No.</strong>");
                    columns.Bound(f => f.ProductCustomerCode).Width(90)
                           .HeaderTemplate("<strong>Customer</br>Code</strong>")
                           .FooterTemplate("<span id='sum'></span>")
                           .HtmlAttributes(new { style = "text-align: center;" });
                    //columns.Bound(f => f.ProductCode)
                    //    .HeaderTemplate("<strong>Mã SP</strong>")
                    //    .HtmlAttributes(new { style = "text-align: center;" })
                    //    .Aggregate(agg => agg.Count())
                    //    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Count) #>")
                    //    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Count) #>");
                    columns.Bound(f => f.ProductCode).Width(100)
                        .HeaderTemplate("<strong>Product</br>Code</strong>")
                        .HtmlAttributes(new { style = "text-align: center;" })
                        .Aggregate(agg => agg.Count())
                        .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Count) #>")
                        .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Count) #>");
                    columns.Bound(f => f.ProductPrice).Width(100)
                           .HeaderTemplate("Unit</br>Price(VND)")
                           .Format("{0:n0}")
                           .HtmlAttributes(new { style = "text-align: right;" });
                    // so lieu
                    columns.Bound(f => f.EarlyQuantity).Title("First</br>Inv").Width(80)
                           .Format("{0:n0}")
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .HtmlAttributes(new { style = "text-align: right;border-left:black 3px double;" })
                           .FooterHtmlAttributes(new { style = "text-align: right;border-left:black 3px double;" });
                    columns.Bound(f => f.EarlyPrice).Title("First</br>Price").Width(90)
                           .Format("{0:n0}")
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .HtmlAttributes(new { style = "text-align: right;border-right:black 3px double;" })
                           .FooterHtmlAttributes(new { style = "text-align: right;border-right:black 3px double;" });
                    //
                    columns.Bound(f => f.ImportQuantity).Title("Packing</br>Entry").Width(70)
                           .Format("{0:n0}")
                           .HtmlAttributes(new { style = "text-align: right;" })
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .FooterHtmlAttributes(new { style = "text-align: right;" });
                    columns.Bound(f => f.ImportReturn).Title("Return</br>Entry").Width(70)
                           .Format("{0:n0}")
                           .HtmlAttributes(new { style = "text-align: right;" })
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .FooterHtmlAttributes(new { style = "text-align: right;" });
                    columns.Bound(f => f.ImportMore).Title("Addition").Width(70)
                           .Format("{0:n0}")
                           .HtmlAttributes(new { style = "text-align: right;" })
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .FooterHtmlAttributes(new { style = "text-align: right;" });
                    columns.Bound(f => f.ImportInternal).Title("Internal</br>Entry").Width(70)
                           .Format("{0:n0}")
                           .HtmlAttributes(new { style = "text-align: right;" })
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .FooterHtmlAttributes(new { style = "text-align: right;" });
                    columns.Bound(f => f.DiffImport).Title("Others</br>Entry").Width(70)
                           .Format("{0:n0}")
                           .HtmlAttributes(new { style = "text-align: right;" })
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .FooterHtmlAttributes(new { style = "text-align: right;" });

                    columns.Bound(f => f.TotalImport).Width(80)
                           .HeaderTemplate("Total</br>Entry</br>(pcs)")
                           .Format("{0:n0}")
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .HtmlAttributes(new { style = "text-align: right;" })
                           .FooterHtmlAttributes(new { style = "text-align: right;" });
                    columns.Bound(f => f.ImportPrice).Title("Entry</br>Price").Width(90)
                           .Format("{0:n0}")
                           .HtmlAttributes(new { style = "text-align: right;" })
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .HtmlAttributes(new { style = "text-align: right;border-right:black 3px double;" })
                           .FooterHtmlAttributes(new { style = "text-align: right;border-right:black 3px double;" });
                    //
                    columns.Bound(f => f.ExportQuantity).Title("Quantity</br>Release").Width(70)
                           .Format("{0:n0}")
                           .HtmlAttributes(new { style = "text-align: right;" })
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .FooterHtmlAttributes(new { style = "text-align: right;" });
                    columns.Bound(f => f.ExportDestroy).Title("Cancel</br>Release").Width(70)
                           .Format("{0:n0}")
                           .HtmlAttributes(new { style = "text-align: right;" })
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .FooterHtmlAttributes(new { style = "text-align: right;" });
                    columns.Bound(f => f.ExportInternal).Title("Internal</br>Release").Width(70)
                           .Format("{0:n0}")
                           .HtmlAttributes(new { style = "text-align: right;" })
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .FooterHtmlAttributes(new { style = "text-align: right;" });
                    columns.Bound(f => f.DiffExport).Title("Others</br>Release").Width(70)
                           .Format("{0:n0}")
                           .HtmlAttributes(new { style = "text-align: right;" })
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .FooterHtmlAttributes(new { style = "text-align: right;" });

                    columns.Bound(f => f.TotalExport).Width(80)
                           .HeaderTemplate("Total</br>Release</br>(pcs)")
                           .Format("{0:n0}")
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .HtmlAttributes(new { style = "text-align: right;" })
                           .FooterHtmlAttributes(new { style = "text-align: right;" });
                    columns.Bound(f => f.ExportPrice).Title("Release</br>Price").Width(90)
                           .Format("{0:n0}")
                           .HtmlAttributes(new { style = "text-align: right;" })
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .FooterHtmlAttributes(new { style = "text-align: right;" });
                    //
                    columns.Bound(f => f.LastQuantity).Title("Last</br>Quantity").Width(80)
                           .Format("{0:n0}")
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .HtmlAttributes(new { style = "text-align: right;border-left:black 3px double;" })
                           .FooterHtmlAttributes(new { style = "text-align: right;border-left:black 3px double;" });
                    columns.Bound(f => f.LastPrice).Title("Last</br>Price").Width(90)
                           .Format("{0:n0}")
                           .HtmlAttributes(new { style = "text-align: right;" })
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #></div>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .FooterHtmlAttributes(new { style = "text-align: right;" });
                    //
                    columns.Bound(f => f.DiffQuantity).Width(80)
                           .HeaderTemplate("Quantity</br>Diff")
                           .Format("{0:n0}")
                           .Aggregate(agg => agg.Sum())
                           .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                           .HtmlAttributes(new { style = "text-align: right;border-left:black 3px double;" })
                           .FooterHtmlAttributes(new { style = "text-align: right;border-left:black 3px double;" });
                    //columns.Bound(f => f.DiffImport).Width(65)
                    //       .HeaderTemplate("Lệch</br>nhập")
                    //       .Format("{0:n0}")
                    //       .Aggregate(agg => agg.Sum())
                    //       .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    //       .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    //       .HtmlAttributes(new { style = "text-align: right;" })
                    //       .FooterHtmlAttributes(new { style = "text-align: right;" });
                    //columns.Bound(f => f.DiffExport).Width(65)
                    //       .HeaderTemplate("Lệch</br>xuất")
                    //       .Format("{0:n0}")
                    //       .Aggregate(agg => agg.Sum())
                    //       .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    //       .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    //       .HtmlAttributes(new { style = "text-align: right;" })
                    //       .FooterHtmlAttributes(new { style = "text-align: right;" });
                })
                .DetailView(details => details.ClientTemplate(
            Html.Telerik().TabStrip()
                .Name("TabStrip_<#= ProductId #>")
                .SelectedIndex(0)
                .Items(items =>
                       items.Add().Text("Warehouse Release").Content(
                           Html.Telerik().Grid<ExportFormTP_KDModel>()
                               .Name("gridTransactionDetail_<#= ProductId #>")
                                    .DataKeys(key => key.Add(f => f.ExportFormId).RouteKey("exportFormId"))
                                   .DataBinding(dataDindings =>
                                       dataDindings.Ajax()
                                                   .Select("SelectExportByProductId", "Reporting", new { productId = "<#= ProductId #>", month = "<#= Month #>", year = "<#= Year #>" })
                                   )
                              .Columns(columns => {
                                  columns.Bound(f => f.CustomerCode).Width(100)
                                         .Title("Customer");
                                  columns.Bound(f => f.DateCreate).Width(75)
                                         .Title("Delivery Date")
                                         .Format("{0:dd/MM/yyyy}");
                                  columns.Bound(f => f.TransactionCode).Width(100)
                                         .Title("Transaction</br>Code");
                                  columns.Bound(f => f.CompanyTransporter).Width(125)
                                         .Title("Transporter</br>Company");
                                  columns.Bound(f => f.Transporter).Width(125)
                                         .Title("Transporter");
                                  columns.Bound(f => f.CarNumber).Width(100)
                                         .Title("Car</br>Number");
                                  columns.Bound(f => f.TotalQuality).Width(75)
                                         .Title("Total</br>Quality")
                                         .HtmlAttributes(new { style = "text-align: right;" })
                                         .Format("{0:n0}");
                                  columns.Bound(f => f.ModifiedUser).Width(75)
                                         .Title("M-User");
                                  columns.Bound(f => f.ModifiedDate).Title("M-Date").Width(75)
                                         .Format("{0:dd/MM/yyyy HH:mm}");

                              })
                                   .ClientEvents(e => e.OnError("grid_OnError"))
                               .ToHtmlString()))
                .ToHtmlString()
        ))
        .Selectable()
            .Scrollable(f => f.Height(510))
            //.HtmlAttributes(new { style = "width: 950px;" })
            .Resizable(f => f.Columns(true))
            .Reorderable(f => f.Columns(true))
            .Filterable()
            .Sortable()
            .ClientEvents(events => events.OnRowSelect("grid_OnRowSelect")
                                        .OnDataBinding("grid_OnDataBinding")
                                        .OnDataBound("grid_OnDataBound")
                                        .OnError("grid_OnError"))
            .Groupable()
            .Render();
    }
</div>

<!-- windown -->
@{
    Html.Telerik().Window()
        .Name("windowReport")
        .Modal(true)
        .Draggable(true)
        .Content
        (
    @<text>
    <div id="result"></div>
    </text>
)
        //.ClientEvents(e => e.OnLoad("Window_onLoad"));
        .Visible(false)
        .Width(960)
        .Render();
}

<style type="text/css">
    #windowReport {
        /*
            float: right;
            Position: relative;
            */
        min-height: 580px;
        top: 15%;
    }

    #result {
        height: 520px;
        overflow: hidden;
    }

    #ReportBody {
        padding-bottom: 25px;
        margin-bottom: 25px;
    }

    .tb-witdh td {
        width: 55px;
    }
</style>
